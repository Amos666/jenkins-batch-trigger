# Jenkins Pipeline Batch Runner · VSCode 插件 UI 设计说明（线框级）

> 交付范围：**纯 UI 设计 / 高保真线框**，不含扩展实现代码。
> 配套可交互原型：`jenkins-pipeline-batch-wireframe.html`（浏览��直接打开即可点）。
> 决策：**Webview 面板布局**；分组与模板形态：**「过滤即模板」+「命名分组」两者都要**。
> 目标平台：Jenkins；触发参数：**通用 KV 键值对**。

---

## 1. 布局架构（对齐 VSCode 标准模式）

```
┌─ 顶栏：插件名 + 面包屑 + 连接状态灯 + ⚙ 设置 ────────────────────────┐
├──────────────┬───────────────────────────────────────────────────┤
│  左栏 248px  │  工具栏：搜索 + 状态 chips + 自动刷新 + ⟳刷新        │
│              ├───────────────────────────────────────────────────┤
│ ▤ 视图       │                                                   │
│   全部/选中/ │            Pipeline 表格（多选 + 状态徽章）          │
│   运行中     │                                                   │
│              │                                                   │
│ 📁 命名分组  │                                                   │
│   ＋存为分组 │                                                   │
│              │                                                   │
│ 🔖 过滤模板  │                                                   │
│   ＋存为模板 │                                                   │
├──────────────┴───────────────────────────────────────────────────┤
│ 底部操作栏：已选计数 · 活动日志 · 清除选择 · ⚙参数 · ▶批量触发      │
│ 活动日志（可折叠）                                                 │
└──────────────────────────────────────────────────────────────────┘
```

- **入口**：Activity Bar 图标 → 打开主 Webview 面板（Editor 区）。
- **左栏**：固定窄栏，承载「视图切换 / 命名分组 / 过滤模板」三类导航，符合 VSCode 侧边导航心智。
- **中区**：工具栏 + 表格 + 底部操作，是批量操作主战场，空间充裕。
- **模态弹窗**：参数编辑、保存分组、保存模板均用 Webview 内浮层，不跳页。

---

## 2. 左栏三类导航

| 区块 | 作用 | 交互 |
|------|------|------|
| **视图** | 快速切看「全部 / 当前选中 / 运行中」 | 点击切换，计数实时更新；非破坏性，不改变勾选 |
| **命名分组** | 保存「具体哪些 pipeline 被勾选」的集合 | 点分组名 → 一键召回勾选；✕ 删除分组 |
| **过滤模板** | 保存「搜索词 + 状态筛选条件」 | 点模板名 → 一键应用筛选；✕ 删除模板 |

> 两者区别（产品语义）：**分组 = 选了谁**（关注集合），**模板 = 怎么筛**（关注条件）。可组合使用：先套模板筛出 failed，再勾选后存成分组「failed 重跑集」。

---

## 3. 工具栏

| 组件 | 行为 |
|------|------|
| 搜索框 | 按 pipeline 名称/folder 实时过滤；输入即触发，会清空当前模板高亮 |
| 状态 chips | 全部/Running/Success/Failed/Unstable/Aborted，单选过滤 |
| 自动刷新 | 开关 + 间隔（5s/10s/30s/1m）；开启后后台轮询状态 |
| ⟳ 刷新 | 手动立即拉取一次状态 |

---

## 4. Pipeline 表格

| 列 | 内容 |
|----|------|
| ☑ | 行多选，与表头「全选（仅当前可见结果）」联动 |
| Pipeline | 名称 + folder 标签 |
| 状态 | 彩色徽章（Running 带呼吸动画） |
| 上次构建 | `#142` |
| 耗时 | `2m 13s`；进行中为 `—` |
| 最近运行 | `3 分钟前`；进行中为「进行中」 |
| 操作 | 触发 · 日志（单行快速动作） |

### 状态色板（对齐 Jenkins 语义）

| 状态 | 色 | 说明 |
|------|----|------|
| Running（building） | 蓝 + 呼吸 | 构建进行中 |
| Success | 绿 | 成功 |
| Failed | 红 | 失败（用户原话 *field*） |
| Unstable | 黄 | 成功但不稳定 |
| Aborted | 灰 | 被中止 |

选中行有浅蓝底色高亮，与未选行区分。

---

## 5. 核心交互流程

### 5.1 连接配置
顶栏绿灯=已连接；点 ⚙ → 配置 Jenkins URL + API Token（落地用 `SecretStorage` 存）。未连接时灯为红。

### 5.2 筛选 → 批量选择
1. 搜索框 / 状态 chips 过滤，或点左栏视图/模板快速定位。
2. 单行勾选，或表头全选（仅作用于当前可见结果）。
3. 底部「已选 N / 总数」实时计数；可「清除选择」。

### 5.3 保存与复用
- **存为分组**：底部或左栏「＋存为分组」→ 弹窗显示将保存的集合摘要 → 命名保存。之后点左栏分组名一键召回勾选。
- **存为模板**：左栏「＋存为模板」→ 弹窗显示当前筛选条件摘要 → 命名保存。之后点左栏模板名一键应用筛选。

### 5.4 参数自定义（通用 KV）
- 底部「⚙ 参数」→ 弹窗：每行 `KEY = VALUE`，可 + 添加 / ✕ 删除。
- 提供「部署 / 发版 / 跳过测试」快速模板一键灌入。
- 参数为全局参数，随触发下发到所有选中 pipeline。
- 按钮上实时显示参数数量。

### 5.5 批量触发
- **▶ 批量触发**：未勾选则提示；已勾选则对每个选中 pipeline 调 `buildWithParameters`（带 KV），该行立即变 Running、build # +1，活动日志记录「触发 X 参数:…」。
- 单行「触发」= 只触发该行。

### 5.6 实时状态刷新
- **手动**：⟳ 刷新立即轮询。
- **定时**：自动刷新开关 + 间隔，后台轮询，Running 推进到终态时徽章与日志同步更新。
- 建议落地时并发限流 + 失败重试。

---

## 6. 状态机

```
  [idle] ──触发──► Running(building) ──轮询──► Success / Failed / Unstable / Aborted
                                                          │
                                                        再次触发
                                                          ▼
                                                       Running
```

---

## 7. 组件清单（落地实现对应）

| 组件 | 对接 |
|------|------|
| 连接配置 | `SecretStorage` + Jenkins REST |
| 左栏导航 | Webview 内列表，状态存插件本地 |
| pipeline 列表 | `GET /api/json?tree=jobs[...]` |
| 状态徽章 | build `result` / `building` 字段 |
| 参数编辑器 | 动态 KV 表单 → `buildWithParameters` |
| 批量触发 | 并发 `POST /job/{job}/buildWithParameters`（限流） |
| 分组/模板 | `workspaceState` / `globalState` 持久化 |
| 刷新 | 手动 + `setInterval` 轮询 |

---

## 8. 落地对接提示
- **鉴权**：API Token 存 `SecretStorage`，请求带 Bearer；Webview 跨域需插件 host 侧代理。
- **触发接口**：`POST {base}/job/{job}/buildWithParameters` + crumb；无参数 job 用 `/build`。
- **轮询**：取各 job 最新 build 的 `result` 与 `building`。
- **持久化**：分组/模板存 `workspaceState`，按工作区隔离。
- **限流**：批量触发并发上限（如 5），失败可重试/跳过。

---

## 9. 原型验收清单
打开 `jenkins-pipeline-batch-wireframe.html` 逐项点验：
- [ ] 顶栏连接状态灯、⚙ 设置入口
- [ ] 左栏视图切换（全部 / 当前选中 / 运行中）
- [ ] 搜索 + 状态 chips 过滤
- [ ] 单行勾选 / 全选（仅当前可见）/ 清除选择
- [ ] ＋存为分组：弹窗摘要 → 命名 → 左栏出现 → 点击召回勾选 → ✕ 删除
- [ ] ＋存为模板：弹窗摘要 → 命名 → 左栏出现 → 点击应用筛选 → ✕ 删除
- [ ] ⚙ 参数：增删 KV、套用快速模板、保存后按钮显示数量
- [ ] ▶ 批量触发：选中项变 Running、日志记录参数、build # +1
- [ ] ⟳ 手动刷新：running 项随机推进到终态、日志同步
- [ ] 自动刷新开关 + 间隔切换
- [ ] 活动日志展开/收起
